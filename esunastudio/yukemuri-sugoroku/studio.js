const viewer=document.querySelector('#image-viewer');let previous;document.querySelectorAll('[data-zoom]').forEach(a=>a.addEventListener('click',e=>{e.preventDefault();previous=a;const img=viewer.querySelector('img');img.src=a.href;img.alt=a.querySelector('img').alt;viewer.querySelector('p').textContent=img.alt;viewer.showModal();document.body.style.overflow='hidden'}));function closeImage(){viewer.close()}viewer.querySelector('button').onclick=closeImage;viewer.addEventListener('click',e=>{if(e.target===viewer)closeImage()});viewer.addEventListener('close',()=>{document.body.style.overflow='';previous?.focus()});
